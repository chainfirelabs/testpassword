# Images are pulled, never built here. See the top-level README for building.
PWNED_REGISTRY=ghcr.io
PWNED_IMAGE_NAMESPACE=chainfirelabs/testpassword
PWNED_IMAGE_TAG=latest

# Downloads and uploads run as this user; ./data must be writable by it.
PWNED_UID=1000
PWNED_GID=1000

# Interface for the web UI on 80/443. Keep loopback until a real certificate is in place.
PWNED_WEB_BIND=127.0.0.1

# Your bucket. Create it yourself; nothing here does.
PWNED_S3_BUCKET=pwned
PWNED_S3_REGION=us-east-1
# Leave unset for AWS; set an HTTPS URL for any other S3-compatible store.
# PWNED_S3_ENDPOINT=https://s3.example.com
# Set both, or leave both unset to use the AWS credential chain.
# PWNED_S3_ACCESS_KEY=
# PWNED_S3_SECRET_KEY=
